# 开发进度

## 当前阶段

阶段二：上课时间和学生管理，已完成。

## 已完成功能

### 系统基础

- 可直接双击运行的单文件 `index.html`
- 顶部横向导航：课时总表、上课时间管理、学生管理、全部记录、数据管理
- 基于 URL Hash 的单页面切换、当前页状态和浏览器前进后退
- 深绿色、白色背景、浅灰边框和宽松留白的现代简约界面
- 统一按钮、表格、表单、状态标签、空数据、弹窗和成功提示样式
- V4.0 `localStorage` 数据结构及初始化、兼容读取、保存和异常保护方法

### 上课时间管理

- 支持新增、编辑、删除和实时搜索
- 支持星期、开始时间、结束时间和备注字段
- 自动生成“周六 09:00-10:30”形式的上课时间名称
- 校验星期、时间格式及结束时间必须晚于开始时间，不支持跨天时段
- 列表按星期、开始时间和结束时间自然排序，并显示关联学生人数
- 支持完全相同的重复时段，所有关联均使用唯一 ID
- 删除有关联学生的上课时间时显示具体人数，并将学生改为未分配时间
- 删除上课时间不会删除学生或历史记录

### 学生管理

- 支持新增、编辑、删除和按姓名实时搜索
- 支持姓名、初始课时、上课时间、状态和备注字段
- 初始课时支持大于等于 0 的整数或小数，并拒绝空值、负数和非数字
- 支持在读、冻课、流失三种状态
- 上课时间可选择已有时段或未分配上课时间
- 学生列表保持存储顺序，显示统一状态标签和关联时段名称
- 删除入口位于编辑弹窗内，并通过危险确认弹窗二次确认
- 删除学生时同步清理该学生的正常上课、续费、转介绍、比赛和退费记录

### 数据与交互

- 所有新增、编辑和删除均在成功写入 `studentHoursManagerData` 后重绘界面
- 保存失败时保留原列表和当前弹窗，并显示可恢复的错误信息
- 页面刷新或重新打开后可恢复上课时间和学生数据
- 兼容旧版本、缺失集合以及未知顶层和记录字段
- 搜索词仅保存在当前页面状态中，不写入 `localStorage`
- 表单支持 Enter 提交、Escape 取消、焦点陷阱、初始聚焦和关闭后焦点恢复
- 提供简洁成功提示及 `aria-live` 状态播报

## 待开发功能

- 正常上课扣课
- 批量扣课
- 续费记录
- 转介绍记录
- 比赛记录
- 退费记录
- 剩余课时自动计算
- 课时总表的数据统计、筛选和操作
- 学生详情
- 全部记录页面
- JSON 数据导入导出
- 清空数据
- 完整系统验收测试

## 阶段二检查

- 已使用 `file://` 方式在 Chrome 中直接打开 `index.html`
- 上课时间新增、编辑、删除、搜索、清除搜索、取消和刷新恢复正常
- 空时间、结束时间不晚于开始时间等校验正常，自动名称和自然排序正确
- 重复上课时间可正常保存，学生人数统计正确
- 学生新增、编辑、删除、搜索、清除搜索、取消和刷新恢复正常
- 空白姓名、空课时、负数和非数字校验正常，小数初始课时可正确保存
- 在读、冻课、流失三种状态及未分配上课时间显示正常
- 删除有关联学生的上课时间后，学生保留且变为未分配时间
- 向五类记录数组预置关联数据后，删除学生只清理该学生记录，其他学生记录保留
- 旧版本、缺失集合、未知顶层字段和未知记录字段可兼容保留
- 损坏 JSON 不会被自动覆盖，并会锁定本次会话写入
- 模拟 `localStorage` 写入失败时，编辑和删除弹窗均保留且不显示虚假成功
- Enter 提交、Escape 关闭、焦点循环和关闭后焦点恢复正常
- 375px、768px、1024px、1440px 和 1675px 下两张管理页均无页面级横向溢出
- 375px 下表格使用容器级横向滚动，弹窗保持在视口内
- 浏览器控制台无错误或警告，仅加载本地 `index.html`，未请求外部资源
- JavaScript 语法、重复 HTML ID、Git 空白错误和禁用内容静态检查通过

## 已知问题

- 阶段二范围内未发现已知功能问题。
- 正常扣课、续费、比赛、转介绍、退费、余额计算、学生详情、总表统计和数据导入导出按计划留待后续阶段。
