# 开发进度

## 当前阶段

阶段六：数据管理、兼容性和完整检查，已完成。第一版全部需求已实现。

## 已完成功能

### 系统基础

- 可直接双击运行的单文件 `index.html`
- 顶部横向导航和基于 URL Hash 的五页面切换
- 深绿色、白色背景、浅灰边框和宽松留白的现代简约界面
- 统一按钮、表格、表单、状态标签、空数据、弹窗和成功提示
- V4.0 `localStorage` 数据结构及兼容读取、原子保存和异常保护

### 上课时间和学生管理

- 上课时间新增、编辑、删除、搜索、自动名称和自然排序
- 学生新增、编辑、删除、搜索、时段关联和在读、冻课、流失状态管理
- 删除上课时间会解除学生关联，删除学生会清理其五类历史记录
- 学生列表新增只读“剩余课时”列，不提供直接修改入口

### 五类记录管理

- “全部记录”页面提供正常上课、续费、转介绍、比赛和退费五个标签页
- 五类记录均支持新增、编辑、二次确认删除、空数据状态和记录数量
- 记录列表默认按日期倒序排列，同日新加入的记录优先显示
- 正常上课记录保存学生、日期、扣除课时和备注，并显示学生当前上课时间
- 续费记录分别保存购买课时和赠送课时
- 转介绍记录保存赠送课时
- 比赛记录保存扣除课时
- 退费记录分别保存退回购买课时和取消赠送课时
- 新记录日期默认当天；必填课时必须大于 0，可选课时默认 0 且不得为负数
- 所有学生、日期和数字字段均进行 JavaScript 校验并提供字段级错误
- 孤立旧记录显示为“未知学生”，不会在加载时被删除或改写

### 重复正常上课提醒

- 新增正常上课记录时检查相同学生和日期
- 第一次提交只显示重复提示，不写入数据
- 用户再次明确选择“仍然保存”后允许保留多条记录，不自动合并
- 编辑后与其他记录形成重复时排除当前记录并执行相同提醒

### 剩余课时计算

- 建立统一的 `calculateRemainingHours` 纯计算函数
- 计算公式包含初始课时、续费购买、续费赠送、转介绍赠送、正常上课、比赛、退回购买和取消赠送
- 剩余课时不写入学生对象或 `localStorage`，每次显示时从完整数据实时计算
- 修改初始课时或新增、编辑、删除任意记录后立即重绘计算结果
- 兼容旧数据中的数字字符串；缺失、负数和非有限旧值按 0 参与计算但不改写原记录
- 计算结果统一到最多 6 位小数，避免浮点尾数
- 剩余课时大于 5 显示深绿色，大于 0 且不超过 5 显示橙色，等于或小于 0 显示红色，负数额外加粗

### 课时总表

- 默认首页已接入四个实时统计卡片：在读学生、本月扣课、课时预警和本月续费
- “本月”卡片按浏览器当前自然月统计，不随表格月份筛选变化
- 支持按上课时间、学生状态、月份和学生姓名组合筛选，状态默认“在读”，月份默认当前月
- 上课时间、状态和姓名只影响学生行；月份只影响正常上课日期列
- 根据所选月份实际存在的正常上课记录动态生成日期列，并按日期升序排列
- 同一学生同一天的多条正常上课记录在日期单元格中合并显示扣课总数
- 比赛、转介绍和续费列显示全部历史次数与累计课时，不受月份筛选影响
- 比赛、转介绍和续费摘要可打开只读弹窗查看全部历史明细
- 剩余课时继续依据初始课时与全部历史记录实时计算，不受筛选和月份影响
- 总表操作列可直接为学生扣课、添加续费/转介绍/比赛/退费记录或编辑学生
- 表格固定选择列和姓名列，其余字段在窄屏或日期较多时通过表格容器横向滚动

### 批量扣课

- 可先按上课时间筛选，再勾选当前列表中的一个或多个在读学生
- 表头支持全选当前可见在读学生，冻课和流失学生的选择框禁用
- 批量表单包含日期、每人扣除课时和备注，数字字段必须为大于 0 的有限数值
- 保存时为每名学生分别生成一条正常上课记录，并通过一次 `saveData` 原子写入
- 若部分学生在同一天已有正常上课记录，第一次提交只提示重复学生，再次明确确认后才继续
- 批量保存成功后立即刷新统计卡片、日期合计、剩余课时、学生列表和全部记录

### 学生详情

- 课时总表、学生管理和全部记录中的学生姓名均可打开大尺寸学生详情弹窗
- 课时总表操作列新增“详情”，比赛、转介绍和续费摘要直接打开详情中的对应标签
- 顶部集中显示姓名、状态、上课时间、备注和八项课时统计
- 退费扣除同时显示退回购买课时与取消赠送课时分项，剩余课时继续按全部历史实时计算
- 详情包含正常上课、续费、转介绍、比赛和退费五个键盘可切换标签
- 正常上课标签默认筛选当前月份，其他四类标签始终显示全部历史记录
- 详情中的每条记录均可编辑和二次确认删除，完成或取消后返回原学生、原标签和原月份
- 详情头部支持编辑学生和添加五类记录；详情内记录固定关联当前学生
- 在编辑学生中删除当前学生后关闭详情，不恢复已经不存在的学生上下文

### 全部记录页面

- 保留五类记录标签、新增、编辑、删除和日期倒序，并补充完整组合筛选
- 支持按学生、当前上课时间、开始日期、结束日期和备注关键词筛选
- 日期范围包含起止当天，开始日期晚于结束日期时显示字段错误并阻止错误筛选
- 备注搜索忽略首尾空格及英文大小写，所有筛选条件按交集计算
- 上课时间筛选依据关联学生当前时段；时段失效、未分配或未知学生归入“未分配上课时间”
- 孤立兼容记录可通过“未知学生（兼容记录）”查看，不会在筛选或加载时被删除
- 标签数字保持显示总记录数，列表数量显示当前筛选结果与总数
- 无原始数据、无筛选结果和日期范围错误分别提供明确空状态与恢复操作

### 数据管理与兼容恢复

- 数据管理页面提供完整 JSON 导出、JSON 覆盖导入和清空全部数据
- 导出文件名使用 `学生课时数据备份_YYYY-MM-DD.json`，内容包含七个正式集合及所有未知兼容字段
- 导出操作只读取当前完整快照，不修改 `localStorage`
- 导入先在内存中完成文件读取、JSON 解析、V4.0 核心字段校验和兼容规范化，再显示文件名及数据数量
- 导入确认明确提示将覆盖当前全部数据；文件选择取消或任何校验失败均不会修改当前数据
- 导入和清空只有在一次 `localStorage.setItem` 成功后才替换内存快照和重绘界面
- 有效备份可以解除损坏本地 JSON 触发的写入锁，恢复后普通新增、编辑和删除继续可用
- 缺失集合会补为空数组，旧数字字符串、孤立关联和未知顶层或记录字段均继续兼容保留
- 清空需要危险确认弹窗并准确输入“确认清空”，成功后保存 V4.0 空结构而不是删除存储键
- 整库导入或清空成功后会重置非持久化筛选、勾选和详情上下文，并重绘五个页面

### 数据与交互

- 五类记录由统一配置驱动，共用表单、校验、列表和 CRUD 流程
- 所有修改只有成功写入 `studentHoursManagerData` 后才重绘界面
- 保存失败时保留原列表和当前弹窗，并显示可恢复错误
- 编辑记录保留原 ID 和未知兼容字段，顶层未知字段同样保留
- 记录标签支持鼠标以及方向键、Home、End 切换
- 表单支持 Enter 提交、Escape 取消、焦点陷阱和关闭后焦点恢复

## 待开发功能

- 第一版需求已全部完成，当前没有需求文档范围内的待开发功能。

## 阶段六检查

- JavaScript 语法、Git 空白、HTML ID 唯一性、外部资源和禁止项静态检查通过
- 使用本机 Chrome 与 Edge 以 `file://` 直接打开，无需服务器或项目依赖
- 验证上课时间和学生新增、编辑、删除，删除上课时间后关联学生正确变为未分配
- 验证五类记录全部新增、编辑和删除按钮，共用流程与各自数字字段均可正常保存
- 使用固定数据验证完整余额公式结果为 `26.5`，同日 `1 + 0.5` 正常上课在总表显示为 `-1.5`
- 验证完整 JSON 文件名、七个集合、未知顶层字段和未知记录字段均正确导出
- 验证无效 JSON、错误集合类型和错误清空短语不会改变当前 `localStorage`
- 验证导出、清空、刷新、重新导入后全部数据、未知字段和余额完整恢复
- 模拟 `localStorage` 写入失败，确认内存快照与原始存储均保持不变
- 使用损坏本地 JSON 验证普通写入和导出会安全停止，有效备份可受控覆盖并解除写入锁
- 验证缺失集合、旧数字字符串、孤立时段关联和未知字段的兼容导入
- Chrome 自动回归通过 24 个核心断言及 42 个扩展断言，Edge 完成导入、刷新和控制台冒烟检查
- 验证 375px、768px、1024px、1440px、1675px 下五个页面均无页面级横向溢出，宽表在自身容器内滚动
- 对照 `ui-reference.png` 检查横向导航、深绿色主色、白色卡片、浅灰边框、留白、表格和危险状态样式
- 本轮 Chrome 与 Edge 控制台均无 JavaScript 错误或警告

## 阶段五检查

- JavaScript 语法、Git 空白、HTML ID 唯一性和禁止项静态检查通过
- 使用本机 Chrome 以 `file://` 直接打开并完成学生详情及全部记录真实交互回归
- 验证学生详情八项统计与完整余额公式，购买、赠送和退费分项保持独立
- 验证五个详情标签、当前月正常上课筛选、其他记录全部历史及方向键、Home、End 切换
- 验证详情记录编辑、删除、添加和编辑学生后返回原标签与月份
- 验证删除当前学生后详情正确关闭，关联五类记录按既有规则清理
- 验证总表比赛、转介绍和续费摘要直接进入对应详情标签
- 验证全部记录的学生、当前时段、日期范围、备注搜索及组合筛选
- 验证日期范围包含边界、反向范围错误、重置筛选和默认日期倒序
- 验证未知学生兼容记录和未分配时段筛选，不改写未知顶层字段
- 模拟 `localStorage` 写入失败，验证详情表单保留、原记录不变且无虚假成功
- 验证详情焦点恢复、弹窗内部滚动及 375px、768px、1024px、1440px、1675px 无页面级横向溢出

## 阶段四检查

- JavaScript 语法、Git 空白、HTML ID 唯一性和禁止项静态检查通过
- 使用本机 Chrome 以 `file://` 直接打开 `index.html`，页面加载及控制台检查通过
- 验证四个统计卡片的学生数、课时合计、预警数和续费笔数
- 验证上课时间、状态、月份、姓名搜索及组合筛选，默认状态与月份正确
- 验证切换月份只改变正常上课日期列，统计卡片、历史摘要和剩余课时保持正确口径
- 验证同一学生同一天 `1 + 0.5` 课时合并显示为 `-1.5`
- 验证跨年份比赛、转介绍和续费累计摘要及全部历史弹窗
- 验证批量全选、冻课/流失禁选、数字校验、重复提醒、二次确认和一人一条记录
- 验证批量保存后立即重算、刷新页面后恢复、未知顶层字段继续保留
- 模拟 `localStorage` 写入失败，验证不产生部分记录、弹窗保留且学生选择状态可恢复
- 验证总表快捷扣课、添加记录和编辑学生时能够预选目标学生
- 验证 375px、768px、1024px、1440px 和 1675px 均无页面级横向溢出，月度表格在自身容器内横向滚动；桌面与移动端视觉布局正常

## 阶段三检查

- JavaScript 语法和 Git 空白检查通过
- 85 个静态 HTML ID 无重复，五个记录标签及必要容器完整
- 确认未存储或直接赋值 `remainingHours`
- 使用固定数据验证余额公式结果为 `26.5`
- 验证 `0.3 - 0.1 - 0.2` 结果归一为 `0`
- 验证有效闰日、无效日期和日期倒序规则
- 验证五类记录配置与五个 V4.0 存储数组一一对应
- 使用模拟表单和内存存储验证五类新增、续费/退费字段分离及续费编辑
- 验证重复正常上课第一次不保存、再次确认后保存
- 验证编辑保留未知记录字段和未知顶层字段
- 验证删除只移除目标记录，保存失败时原数据和确认弹窗保持不变
- 验证阶段三余额保存路径示例结果为 `28`
- 未发现外部正式资源、存储自动清理调用或剩余课时写入字段

## 已知问题与验证限制

- 最终阶段代码、数据逻辑和已覆盖的浏览器流程未发现功能性已知问题。
- 自动化使用独立的临时浏览器数据，不会读取或修改用户日常浏览器中的正式 `localStorage` 数据。
- 此前个别 `file://` 浏览器环境出现过 SVG 片段的 unique origin 安全提示；本轮本机 Chrome 与 Edge 均未复现，且没有发现数据或交互功能受影响。若特定浏览器扩展或预览容器再次输出该提示，应以直接双击文件后的实际功能和无 JavaScript `error` 为判断依据。
