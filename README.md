# ClassLedger 学生课时管理系统

ClassLedger 是一个供老师个人使用的本地学生课时管理系统，用于替代传统 Excel 课时表。

系统采用单文件结构，只需双击 `index.html` 即可在 Chrome、Edge 等现代浏览器中使用。所有正式功能均使用 HTML、CSS 和原生 JavaScript 实现，不需要服务器、数据库、Node.js、外部框架、CDN 或网络连接。

![ClassLedger 界面设计参考](./ui-reference.png)

## 当前状态

当前已完成：**阶段二：上课时间和学生管理**。

已验证功能：

- 顶部横向导航、五个页面切换、URL Hash 路由和刷新恢复
- 上课时间新增、编辑、删除、搜索、自动名称、自然排序和学生人数统计
- 学生新增、编辑、删除、姓名搜索、初始课时、时段关联和三种状态管理
- 删除上课时间后自动解除学生关联，删除学生时清理其五类历史记录
- 所有阶段二修改立即保存到 `localStorage`，重新打开页面后恢复
- 旧版本、缺失集合、未知字段、损坏 JSON 和写入失败保护
- 统一弹窗、字段校验、成功提示、键盘操作和焦点恢复
- 375px 至 1675px 的响应式布局及表格容器级横向滚动

当前尚未实现正常扣课、批量扣课、续费、比赛、转介绍、退费、余额计算、学生详情、总表统计、全部记录管理和数据导入导出。

下一阶段：**阶段三，具体范围以新的阶段任务为准**。

详细进度请查看 [PROGRESS.md](./PROGRESS.md)。

## 快速开始

1. 保持项目文件位于同一文件夹中。
2. 双击 `index.html`。
3. 选择 Chrome 或 Edge 打开。
4. 先在“上课时间管理”中建立固定时段，再到“学生管理”中添加学生并关联时段。

不需要执行安装命令，也不需要启动本地服务器。

> 数据保存在当前浏览器的 `localStorage` 中。更换浏览器、浏览器配置或文件位置时，可能使用不同的本地存储空间。正式数据导入导出功能将在后续阶段实现。

## 阶段二使用说明

### 上课时间管理

- 点击“新增上课时间”，填写星期、开始时间、结束时间和可选备注。
- 名称由系统自动生成，例如“周六 09:00-10:30”。
- 搜索可匹配名称、星期、起止时间和备注。
- 删除仍有学生关联的时段后，学生会保留并显示为“未分配上课时间”。

### 学生管理

- 点击“添加学生”，填写姓名、初始课时、上课时间、状态和可选备注。
- 初始课时必须为大于等于 0 的数字，允许小数。
- 状态可选在读、冻课或流失。
- 删除学生的入口位于“编辑学生”弹窗底部，确认后会同步清理该学生的五类历史记录。

每次新增、编辑或删除成功后都会立即保存，无需手动点击总保存按钮。搜索条件不会保存。

## 页面结构

| 页面 | 当前状态 | 用途 |
| --- | --- | --- |
| 课时总表 | 基础框架完成 | 后续查看学生课时、续费、比赛、转介绍和剩余课时 |
| 上课时间管理 | 阶段二已完成 | 管理星期、开始时间、结束时间、备注和关联学生人数 |
| 学生管理 | 阶段二已完成 | 管理学生资料、初始课时、上课时间和学习状态 |
| 全部记录 | 页面骨架完成 | 后续集中查看五类历史记录 |
| 数据管理 | 存储底座完成 | 后续导出、导入和清空本地数据 |

## 本地数据

存储键名：

```text
studentHoursManagerData
```

基础结构：

```javascript
{
  version: "4.0",
  classTimes: [],
  students: [],
  lessonRecords: [],
  renewalRecords: [],
  referralRecords: [],
  competitionRecords: [],
  refundRecords: []
}
```

阶段二主要记录示例：

```javascript
{
  id: "time_001",
  weekday: 6,
  weekdayName: "周六",
  startTime: "09:00",
  endTime: "10:30",
  displayName: "周六 09:00-10:30",
  note: ""
}

{
  id: "student_001",
  name: "示例学生",
  initialHours: 17,
  classTimeId: "time_001",
  status: "active",
  note: "",
  createdAt: "2026-07-30T20:00:00.000Z"
}
```

数据原则：

- 页面启动时不得自动清空或覆盖已有数据。
- 旧数据结构必须兼容读取并保留已有记录和未知字段。
- 所有新增、编辑和删除只有在成功写入后才更新界面。
- 购买课时和赠送课时必须分开保存。
- 剩余课时只能根据全部历史记录计算，不能直接修改。
- 月份筛选只影响正常上课日期列。
- 比赛、转介绍和续费始终显示全部历史记录。

## 技术与界面约束

- 所有正式功能代码放在 `index.html` 中。
- 只使用 HTML、CSS 和原生 JavaScript。
- 不使用后端、数据库、Node.js、外部框架、CDN 或联网资源。
- 不开发登录、账号、头像、老师姓名、通知和权限功能。
- 使用深绿色主色、白色背景、浅灰边框、轻圆角和大量留白。
- 删除、清空和覆盖导入操作必须二次确认。
- 所有数字输入必须校验。

完整需求请查看 [REQUIREMENTS.md](./REQUIREMENTS.md)。

## 项目文件

```text
ClassLedger/
├─ index.html          # 系统全部正式功能代码
├─ README.md           # 项目说明与阶段概览
├─ REQUIREMENTS.md     # 完整需求与验收标准
├─ PROGRESS.md         # 详细开发进度
├─ AGENTS.md           # 开发约束与工作流程
└─ ui-reference.png    # 界面设计参考图
```

## 阶段维护规则

每完成一个新阶段，必须同步更新：

1. 本 README 的“当前状态”“已验证功能”“下一阶段”和必要的使用说明。
2. `PROGRESS.md` 中的当前阶段、已完成功能、测试结果、待开发功能和已知问题。
3. 与数据结构、使用方式或项目文件有关的其他说明。

阶段提交说明统一使用：

```text
完成阶段X：阶段名称
```

例如：

```text
完成阶段一：系统基础框架
完成阶段二：上课时间和学生管理
```

## 阶段一测试记录

- 已通过 `file://` 方式在 Chrome 中直接打开。
- 五个页面切换、刷新、前进和后退正常。
- 弹窗打开、关闭和焦点恢复正常。
- 首次初始化、刷新读取和旧数据兼容正常。
- 损坏 JSON 不会被自动覆盖。
- 已检查 375px、768px、1024px、1440px 和 1675px 页面宽度。
- 浏览器控制台无错误或警告，未请求任何外部资源。

## 阶段二测试记录

- 上课时间和学生的新增、编辑、删除、搜索、取消及刷新恢复均通过。
- 时间先后关系、空白姓名、负数、非数字和小数课时校验均通过。
- 自动名称、自然排序、三种学生状态、学生人数和未分配时间显示均通过。
- 删除时段解除学生关联、删除学生清理五类记录且保留其他记录均通过。
- 旧版本、缺失集合、未知字段、损坏 JSON 和模拟写入失败保护均通过。
- Enter、Escape、焦点陷阱、关闭后焦点恢复和成功状态播报均通过。
- 已检查 375px、768px、1024px、1440px 和 1675px 页面宽度。
- Chrome 控制台无错误或警告，仅加载本地 `index.html`，未请求外部资源。
