# 项目说明

这是一个老师个人使用的本地学生课时管理系统。

## 不可违反的要求

1. 最终系统必须可以直接双击 index.html 使用。
2. 只使用 HTML、CSS 和原生 JavaScript。
3. 不使用后端、数据库、Node.js、外部框架、CDN 或联网资源。
4. 所有正式功能代码放在 index.html 中。
5. 数据保存在 localStorage。
6. 不开发登录、用户头像、老师姓名、通知或权限功能。
7. 不显示“欢迎回来，某某老师”。
8. 界面必须参考 ui-reference.png 的现代简约风格。
9. 深绿色为主色，白色背景，浅灰边框，大量留白。
10. 比赛、转介绍和续费显示全部历史记录，不受月份筛选影响。
11. 月份筛选只影响正常上课日期列。
12. 购买课时和赠送课时必须分开保存。
13. 不允许直接修改剩余课时，必须根据所有记录自动计算。
14. 不得在页面启动时自动清空 localStorage。
15. 修改数据结构时必须兼容并保留已有数据。

## 开发流程

1. 开始工作前阅读 REQUIREMENTS.md。
2. 只实现当前任务指定的阶段，不提前开发其他阶段。
3. 修改前先检查现有代码，不要重复实现已有功能。
4. 每阶段完成后进行功能检查。
5. 每阶段完成后更新 PROGRESS.md。
6. 完成后说明：
   - 修改了哪些内容；
   - 如何测试；
   - 是否存在未完成或已知问题。

## 代码要求

1. HTML、CSS、JavaScript 分区清晰。
2. 使用明显的代码注释划分模块。
3. 避免重复代码。
4. 所有数字输入必须校验。
5. 删除、清空和覆盖导入必须二次确认。
6. 所有新增、编辑和删除操作完成后立即保存到 localStorage。
7. 页面重新打开后必须正确恢复数据。
8. 不允许只制作静态界面，按钮和数据功能必须真实可用。